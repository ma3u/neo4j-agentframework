# Neo4j Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# LLM Configuration (choose one)

# Option 1: Ollama (Local LLM - Recommended)
# Install: https://ollama.ai
# Models: ollama pull llama2
LLM_BACKEND=ollama
OLLAMA_MODEL=llama2
OLLAMA_BASE_URL=http://localhost:11434

# Option 2: OpenAI
# LLM_BACKEND=openai
# OPENAI_API_KEY=your_api_key_here
# OPENAI_MODEL=gpt-3.5-turbo

# Option 3: Auto-detect (tries Ollama first, then OpenAI, then fallback)
# LLM_BACKEND=auto

# Option 4: Fallback only (no external LLM, uses smart extraction)
# LLM_BACKEND=fallback